import 'package:flutter/material.dart';

class SavedPropertyCard extends StatelessWidget {
  const SavedPropertyCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
